Quick project generation with Maven => mvn archetype:generate

Run test with Maven => mvn clean compile test

Make jar with Maven => mvn package

make it available for next project on local => mvn install